import { CollectionReference } from './collection';
declare class Transaction {
    _transaction: any;
    _db: any;
    constructor(transaction: any, db: any);
    init(): Promise<any>;
    collection(collName: any): CollectionReference;
    commit(): Promise<any>;
    rollback(reason: any): Promise<any>;
}
declare function runTransaction(callback: any, times?: number): Promise<any>;
declare function startTransaction(): Promise<Transaction>;
export { Transaction, runTransaction, startTransaction };
